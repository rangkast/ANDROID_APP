package com.toadstudio.first.toadproject.RecycleView;

/**
 * Created by rangkast.jeong on 2018-03-07.
 */

import android.support.v7.app.AppCompatActivity;

public class Recycle_Main extends AppCompatActivity {
/*
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.recycle_view_main);

        DisplayMetrics metrics = getDisplayMetrics();
        List<String> metalList = Arrays.asList("\\m/", "\\m/", "\\m/");
        FullMetalAdapter fullMetalAdapter = new FullMetalAdapter(metrics, metalList);

        MetalRecyclerViewPager viewPager = (MetalRecyclerViewPager) findViewById(R.id.layout2_viewPager);
        viewPager.setAdapter(fullMetalAdapter);
    }

    public DisplayMetrics getDisplayMetrics() {
        Display display = getWindowManager().getDefaultDisplay();
        DisplayMetrics metrics = new DisplayMetrics();
        display.getMetrics(metrics);

        return metrics;
    }
    */
}

